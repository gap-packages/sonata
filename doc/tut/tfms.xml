<Chapter Label="Nearrings of transformations on groups">
<Heading>Nearrings of transformations on groups</Heading>
We are going to study transformations on the alternating group on four elements
<M>A_4</M>.
<P/>
<E>The problem</E>: Let <A>T</A> be the nearring of mappings from <M>A_4</M> to <M>A_4</M>
        generated by the single mapping <A>t</A> which maps (2,3,4) to (2,4,3),
        (2,4,3) to (1,2)(3,4), (1,2)(3,4) to (1,2,3), (1,2,3) back to (2,3,4)
        and all other elements of <M>A_4</M> to the neutral element (). Then,
        how many mappings are there in <A>T</A> that have (1,2,3) as a fixed point?
        If there are only a few we would be interested in a list of all of
        these.
<P/>
<E>The solution</E>: 
        The first thing to do is create the nearring <A>T</A>. So we start with
        the group <M>A_4</M>, which can easily be constructed with the command
<Example><![CDATA[
gap> A4 := AlternatingGroup( 4 );
Alt( [ 1 .. 4 ] )
]]></Example>
The result is an object which represents the group <M>A_4</M>. If we want
        to see its elements we have to ask &GAP; to make a list of elements
        out of the group.
<Example><![CDATA[
gap> AsSortedList( A4 );                                       
[ (), (2,3,4), (2,4,3), (1,2)(3,4), (1,2,3), (1,2,4), (1,3,2),
 (1,3,4), (1,3)(2,4), (1,4,2), (1,4,3), (1,4)(2,3) ]
]]></Example>
Now we create the mapping <A>t</A>. We use the function
        <C>MappingByPositionList</C> to enter it.
<Example><![CDATA[
t := EndoMappingByPositionList( A4, [1,3,4,5,2,1,1,1,1,1,1,1] );
<mapping: AlternatingGroup( [ 1 .. 4 ] ) -> AlternatingGroup( 
[ 1 .. 4 ] ) >
]]></Example>
For <C>Mappings</C> the usual operations <C>+</C> and
        <C>*</C> can be used to add and multiply them.
<Example><![CDATA[
gap> t+t;
<mapping: AlternatingGroup( [ 1 .. 4 ] ) -> AlternatingGroup( 
[ 1 .. 4 ] ) >
gap> last * t;
<mapping: AlternatingGroup( [ 1 .. 4 ] ) -> AlternatingGroup( 
[ 1 .. 4 ] ) >
]]></Example>
(Recall that <C>last</C> stands for the result of the last computation, in
        this case this is <C>t + t</C>). 
        Now we can construct the nearring. We use the function
        <C>TransformationNearRingByGenerators</C> which asks for the group (<M>A_4</M>)
        and a list of generating elements (the list with <A>t</A> as the only entry)
        as arguments.
<Example><![CDATA[
gap> T := TransformationNearRingByGenerators( A4, [ t ] );;
]]></Example>
Nearrings, although generated by a single element, can become rather
        big. Before we print out all elements we ask for the size of <A>T</A>.
<Example><![CDATA[
gap> Size( T );
20736
]]></Example>
It seems reasonable not to print all elements. <E>Note</E> that they are
        not even computed, yet. All we wanted to know was the size of <A>T</A> and
        this can be computed without generating all elements. But, yes, we
        could generate them with <C>AsList</C> or <C>AsSortedList</C>. At last we want
        to find out how many of these 20736 <C>GroupTransformations</C> have (1,2,3)
        as a fixed point. We filter them out, but we use a second semicolon at
        the end to suppress printing, because there might be a lot of them.
        Then we ask for the length of the resulting list <A>F</A> of mappings.
<Example><![CDATA[
gap> F := Filtered( T, tfm -> Image( tfm, (1,2,3) ) = (1,2,3) );;
gap> Length( F );
1728
]]></Example>
It seems not to be worth printing the whole list. But we could for
        example choose a random transformation from this list <A>F</A> for testing
        purposes.
<Example><![CDATA[
gap> Random( F );;
]]></Example>
There are of course other properties of the nearring <A>T</A>
        which might be interesting. It is clear that a nearring which is
        generated by a single element is not necessarily abelian. <A>T</A> is a
        counterexample. As for finding counterexamples, SONATA can be used
        as a research tool.
<Example><![CDATA[
gap> IsCommutative( T );
false
]]></Example>
Finally, we try to disprove the conjecture that every transformation
        nearring on an abelian group that is generated by a single element 
        must be commutative.
<Example><![CDATA[
gap> g := CyclicGroup(2);;
gap> m := MapNearRing(g);;
gap> Filtered( m, n -> not( IsCommutative(                                            
>        TransformationNearRingByGenerators( g, [n] ) ) ) );
gap> [ <mapping: Group( [ f1 ] ) -> Group( [ f1 ] ) >, 
>        <mapping: Group( [ f1 ] ) -> Group( [ f1 ] ) > ]
gap> GraphOfMapping(last[1]);
[ [ <identity> of ..., f1 ], [ f1, <identity> of ... ] ]
]]></Example>
</Chapter>
